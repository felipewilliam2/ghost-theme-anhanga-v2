<header class="site-header">
    <div class="container">
        <a href="{{@site.url}}" class="site-logo">
            {{#if @site.logo}}
                <img src="{{@site.logo}}" alt="{{@site.title}}" style="max-height: 40px;">
            {{else}}
                Anhangá Viagens
            {{/if}}
        </a>
        <nav class="site-nav">
            <a href="https://anhanga.tur.br">Início</a>
            <a href="{{@site.url}}">Blog</a>
            <a href="https://anhanga.tur.br/orcamento" style="background: var(--color-primary); color: white; padding: 0.5rem 1.5rem; border-radius: 9999px;">Orçamento</a>
        </nav>
    </div>
</header>
